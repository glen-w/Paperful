# Optional local Compose overrides (gitignored as compose.override.yaml).
#
# Prefer editing .env for data location:
#   PAPERFUL_DATA=../paperful-data   # sibling dir (recommended)
#   PAPERFUL_DATA=.                 # keep config/out/state in the repo
#
# Example: pin Zotero host explicitly
#
# services:
#   paperful:
#     environment:
#       PAPERFUL_ZOTERO_HOST: host.docker.internal
